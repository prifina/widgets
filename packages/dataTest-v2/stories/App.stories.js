import React, { useState, useEffect } from "react";
import DataTest from "../src/App";

export default { title: "DataTest" };

export const app = () => {
  return <App data={{}} />;
};
app.story = {
  name: "DataTest",
};
